import Link from "next/link";

export default async function ForgotPassword({searchParams}:{searchParams:Promise<{enviado?:string;erro?:string}>}){const params=await searchParams;return <main className="form-wrap"><form className="form-card" action="/api/auth/forgot-password" method="post"><Link href="/entrar" style={{fontSize:12,color:"var(--muted)"}}>← Voltar para entrar</Link><span className="eyebrow" style={{display:"block",marginTop:25}}>Recuperar acesso</span><h1>Vamos criar uma nova senha.</h1>{params.enviado?<p>Se este e-mail estiver cadastrado, você receberá um link para redefinir a senha.</p>:<><p>Informe seu e-mail e enviaremos um link seguro para você voltar ao seu espaço.</p><div className="form-field"><label htmlFor="reset-email">E-mail</label><input id="reset-email" name="email" type="email" autoComplete="email" required/></div><button className="btn" style={{width:"100%"}}>Enviar link</button></>}{params.erro&&<p className="form-error">Confira o e-mail e tente novamente.</p>}</form></main>}
